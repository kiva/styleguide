{{!This is the structure that should be used for all lightboxes. Steps to create a new lightbox are as follows:
1: Create a new molecule file within the 10-lightbox folder titled appropriately for your new lightbox
2: Copy everything in this file.
3: Paste this structure within your new lightbox file
4: Add your lightbox specific content within the lightboxContent div of this code structure, everything else should stay the same }}

<a href="#" class="lightbox-trigger-link" data-reveal-id="{{dataRevealId}}">{{lightboxTriggerLinkText}}</a>
<div id="{{dataRevealId}}" class="{{neededClass}} lightbox-wrapper" data-reveal aria-hidden="true" role="dialog">
    <div class="row">
        <div class="columns small-12">
            <a class="close-reveal-modal" aria-label="Close">
                <svg class="icon icon-x">
                    <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-x"></use>
                </svg>
            </a>
            <div class="lightbox-content white">
                {{! Add your specific lightbox content here, you shouldn't have to touch the other lines of code wihtin this file.}}
            </div>
        </div>
    </div>
</div>