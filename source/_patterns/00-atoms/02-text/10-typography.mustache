<div class="sg-type-size-head">Small text / Footer text</div>
<p class="small-text">{{excerpt.short}}</p>

<div class="sg-type-size-head">Body text</div>
<p>{{excerpt.short}}</p>

<div class="sg-type-size-head">Featured text</div>
<p class="featured">{{excerpt.short}}</p>

<div class="sg-type-size-head">Overlay text</div>
<p class="overlay">{{excerpt.short}}</p>

<div class="sg-type-size-head" style="background-color: inherit">Top Nav</div>
<p class="top-nav">{{nav-text}}</p>

<div class="sg-type-size-head">Secondary Nav</div>
<p class="secondary-nav">Fund a loan. Get repaid. Fund another.</p>

<div class="sg-type-size-head">Section head (h2)</div>
<h2>{{headline.short}}</h2>

<div class="sg-type-size-head">Headline (h1)</div>
<h1>{{headline.short}}</h1>

<div class="sg-type-size-head">Impact text</div>
<p class="impact">{{headline.short}}</p>

<div class="sg-type-size-head">Huge Headline (h1.huge)</div>
<h1 class="huge">What makes us unique</h1>

<div class="sg-type-size-head">Statistic</div>
<p class="statistic">{{decimal.medium}}</p>

<div class="sg-type-size-head">Huge Statistic</div>
<p class="huge-statistic">75%</p>


