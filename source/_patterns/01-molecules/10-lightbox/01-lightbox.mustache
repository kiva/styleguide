<a href="#" data-reveal-id="modal">Testing{{modal-trigger-text}}</a>

<section class="lightbox">
	<div id="modal" class="reveal-modal" data-reveal aria-labelledby="modalTitle" aria-hidden="true" role="dialog">


			<h1 id="modalTitle" class="white">title{{modal-title}}</h1>

        <div class="flex-video widescreen vimeo">
            <iframe width="{{youtube-video.width}}" height="{{youtube-video.height}}" src="{{youtube-video.src}}" frameborder="{{youtube-video.frameborder}}" allowfullscreen></iframe>
        </div>

		<p class="featured white text-left">text{{modal-text}}</p>

		<a class="close-reveal-modal" aria-label="Close">
			<svg class="icon icon-x">
				<use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-x"></use>
			</svg>
		</a>
	</div>
</section>